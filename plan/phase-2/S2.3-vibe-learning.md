# S2.3 — Vibe Learning Loop

**Sprint:** Phase 2 · Intelligence
**Sessions:** 2
**Goal:** User behavior updates vibe profiles, making the feed more accurate over time.

---

## Session 6 — Signal Tracking ✅

### Tasks
- [x] `user_signals` table migration (id, user_id, venue_id, signal_type, created_at)
- [x] RLS policies: users can only insert/read own signals
- [x] Signal type definitions in `types.ts`
- [x] Signal queries: `recordSignal()`, `getSignalCounts()`, `getUserSignals()`
- [x] `POST /api/signals` route with Zod validation
- [x] `track-signal.ts` client utility (sendBeacon for view/share, fetch for others)
- [x] Wired into VenueDetail: view (on mount), save/unsave, neyece tap, share

## Session 7 — Profile Vector Drift ✅

### Tasks
- [x] `vibe-learner.ts`:
  - `computeVectorDrift()` — shifts user vector based on signal weights
    - neyece=3x, save=2x, share=2x, view=0.5x, unsave=-0.5x
    - Learn rate 0.05, decay rate 0.01, max magnitude 2.0
  - `shouldUpdateProfile()` — >= 5 signals or >= 24h with >= 1 signal
- [x] `POST /api/profile/drift` route:
  - Fetches signals since last update
  - Computes vector drift
  - Upserts new fingerprint vector
  - Invalidates cached scores
  - Returns dimensions shifted count

### Files Created/Modified
| File | Description |
|------|-------------|
| `supabase/migrations/00003_user_signals.sql` | Signal tracking table + RLS |
| `src/lib/supabase/types.ts` | Added user_signals table type |
| `src/lib/supabase/queries.ts` | Signal queries |
| `src/app/api/signals/route.ts` | Signal tracking API |
| `src/lib/track-signal.ts` | Client-side fire-and-forget tracker |
| `src/lib/vibe-learner.ts` | Vector drift computation |
| `src/app/api/profile/drift/route.ts` | Profile drift API |
| `src/components/discover/VenueDetail.tsx` | Wired signal tracking |

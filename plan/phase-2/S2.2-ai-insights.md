# S2.2 — AI Insight Generation

**Sprint:** Phase 2 · Intelligence
**Sessions:** 2
**Goal:** Claude API generates personalized "Why this is Neyece for you" insights.

---

## Session 4 — Claude API Integration ✅

### Tasks
- [x] Install `@anthropic-ai/sdk`
- [x] `insights.ts` service:
  - `generateInsight()` — calls Claude (claude-sonnet-4-6), warm brand voice, max 100 tokens
  - `templateInsight()` — fallback template (same as Phase 1 placeholder)
  - `extractTopVibes()` — top 3-4 dimensions from user vector
  - `findMatchedDimensions()` — shared positive dimensions between user + venue
- [x] `GET /api/insights/[venueId]` — auth required, 1h cache header
- [x] Graceful fallback: no API key → template, API error → template

## Session 5 — Insight UI ✅

### Tasks
- [x] `VibeInsight` client component: renders template instantly, lazy-fetches AI version on mount
- [x] Smooth fade transition when AI insight replaces template
- [x] "Powered by Neyece AI" label when AI source
- [x] VenueDetail updated to use `<VibeInsight>` instead of static block
- [x] Never shows error to user

### Files Created/Modified
| File | Description |
|------|-------------|
| `src/lib/insights.ts` | Claude API integration + template fallback |
| `src/app/api/insights/[venueId]/route.ts` | Insight API endpoint |
| `src/components/discover/VibeInsight.tsx` | Lazy-loading insight component |
| `src/components/discover/VenueDetail.tsx` | Wired VibeInsight component |
| `src/components/discover/index.ts` | Added VibeInsight export |

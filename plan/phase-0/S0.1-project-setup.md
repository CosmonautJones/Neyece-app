# S0.1 — Project Setup

**Sprint:** Phase 0 · Foundation
**Sessions:** 3
**Goal:** Fully scaffolded Next.js 14 app deployed to Vercel with Supabase connected.

---

## Session 1 — Scaffold & Config ✅

**Focus:** Initialize the project, install deps, configure tooling.

### Tasks
- [x] `npx create-next-app@14` with TypeScript, App Router, Tailwind, ESLint
- [x] Configure `tailwind.config.js` with Neyece brand tokens:
  - Colors: coral `#FF6B6B`, yellow `#FFD93D`, cream `#FFF8F0`, dark `#2D2D2D`
  - Fonts: Fraunces (display), Nunito (body)
- [x] Set up project structure: `/components`, `/lib`, `/scripts`, `/supabase/migrations`
- [x] Add `.env.local.example` with all required env var keys (no values)
- [x] Install core deps: `@supabase/supabase-js`, `@clerk/nextjs`, `zod`
- [x] Configure ESLint + Prettier with project rules
- [x] Create root layout with font loading + metadata

### Deliverables
- ✅ Running `npm run dev` shows a styled Neyece placeholder page
- ✅ All brand tokens accessible via Tailwind classes (`bg-coral`, `text-cream`, etc.)

---

## Session 2 — Database & Supabase ✅

**Focus:** Supabase project setup, schema migrations, RLS policies.

### Tasks
- [ ] Create Supabase project (manual step — need project URL + anon key)
- [x] Write SQL migrations for Phase 0 tables:
  - `users` (id UUID PK, clerk_id unique, email unique, name, city, created_at, vibe_profile JSONB, onboarded)
  - `venues` (id UUID PK, name, neighborhood, city, lat, lng, category, google_place_id, vibe_tags JSONB, description, image_url, price_level, hours)
  - `vibe_profiles` (user_id FK, answers JSONB, fingerprint_vector DOUBLE PRECISION[], updated_at)
  - `neyece_scores` (venue_id FK, user_id FK, score 0–100, computed_at)
  - `saved_spots` (user_id FK, venue_id FK, saved_at, notes)
  - `waitlist` (email unique, city, created_at, converted boolean)
- [x] Write RLS policies: users can read/write their own data, venues are public read
- [x] Create Supabase client utility in `/lib/supabase/` (server + client variants)
- [x] Add database types (hand-written, swap to `supabase gen types` when connected)
- [ ] Test connection: seed 1 test venue, query it from a server component

### Deliverables
- ✅ All 6 tables defined with proper indexes and RLS (`supabase/migrations/`)
- ✅ Supabase client works in both server and client components (`src/lib/supabase/`)
- ✅ Type-safe database queries via TypeScript types (`src/lib/supabase/types.ts`)

### Manual steps remaining
- Create Supabase project and run migrations in SQL editor
- Add `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` to `.env.local`

---

## Session 3 — Deploy & CI (partial ✅)

**Focus:** Vercel deployment, environment config, basic CI.

### Tasks
- [ ] Connect repo to Vercel project *(manual)*
- [ ] Configure environment variables in Vercel dashboard *(manual)*
- [x] Verify production build succeeds (`npm run build`)
- [x] Set up GitHub Actions workflow (`.github/workflows/ci.yml`):
  - Lint on PR ✅
  - Type check on PR ✅
  - Format check on PR ✅
  - Build check on PR ✅
- [x] `.gitignore` entries for `.env.local`, `node_modules`, `.next` (already present)
- [ ] Verify deployed app loads at Vercel URL *(manual)*

### Deliverables
- ✅ CI runs on every PR (lint + type check + format + build)
- ⬜ App deployed and accessible at `*.vercel.app` (needs Vercel setup)
- ⬜ Environment variables configured for prod (needs Vercel dashboard)

### Manual steps remaining
- Connect GitHub repo to Vercel
- Add env vars in Vercel dashboard
- Verify deploy

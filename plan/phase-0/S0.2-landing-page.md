# S0.2 — Landing Page Live

**Sprint:** Phase 0 · Foundation
**Sessions:** 2
**Goal:** Convert HTML landing page to Next.js, wire up waitlist capture, deploy live.

---

## Session 1 — Convert Landing Page to React

**Focus:** Turn `design/landing-page.html` into Next.js components.

### Tasks
- [ ] Break HTML landing page into React components:
  - `LandingNav` — sticky nav with CTA button
  - `HeroSection` — headline, subtext, email signup form
  - `HowItWorks` — 3-step feature explanation
  - `ProblemSolution` — before/after comparison
  - `VibeQuizPreview` — interactive quiz teaser
  - `SocialProof` — testimonials section
  - `CityShowcase` — launch cities preview
  - `MarqueeTagline` — animated "That's Neyece" marquee
  - `Footer` — brand messaging + links
- [ ] Create reusable `NeyeceLogo` component with eye-gradient wordmark
- [ ] Implement email signup form (client component with form state)
- [ ] Mobile-responsive — test at 375px, 768px, 1024px
- [ ] Add smooth scroll navigation between sections

### Deliverables
- Landing page renders identically to HTML mockup
- All sections are individual components
- Email form captures input (not yet wired to backend)

---

## Session 2 — Waitlist Backend + Analytics

**Focus:** Wire email capture to Supabase, add PostHog, go live on domain.

### Tasks
- [ ] Create API route `POST /api/waitlist` — validates email (Zod), inserts to `waitlist` table
- [ ] Wire signup form to API route with loading state + success/error feedback
- [ ] Duplicate email handling: friendly message, no error
- [ ] Install PostHog (`posthog-js`) and initialize in root layout
- [ ] Track events: `waitlist_signup`, `page_view`, `quiz_preview_started`
- [ ] Set up custom domain in Vercel (manual: neyece.com DNS config)
- [ ] Add basic SEO: meta tags, OG image, favicon
- [ ] Test full flow: visit → signup → confirm → check Supabase row

### Deliverables
- Waitlist emails saved to Supabase `waitlist` table
- PostHog tracking live events
- Landing page deployed on production URL
- OG image shows correctly when shared on social

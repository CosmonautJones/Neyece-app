# S0.2 — Landing Page Live

**Sprint:** Phase 0 · Foundation
**Sessions:** 2
**Goal:** Convert HTML landing page to Next.js, wire up waitlist capture, deploy live.

---

## Session 1 — Convert Landing Page to React ✅

**Focus:** Turn `design/landing-page.html` into Next.js components.

### Tasks
- [x] Break HTML landing page into React components:
  - `Navbar` — sticky nav with CTA button
  - `Hero` + `HeroCard` — headline, subtext, venue preview card, email signup
  - `HowItWorks` — 3-step feature explanation
  - `ProblemSolution` — before/after comparison
  - `QuizPreview` — interactive quiz teaser (client component)
  - `SocialProof` — testimonials section
  - `CityShowcase` — launch cities preview
  - `Marquee` — animated "That's Neyece" marquee
  - `BottomCta` — bottom email signup CTA
  - `Footer` — brand messaging + copyright
- [x] Create reusable `NeyeceLogo` component with eye-gradient wordmark
- [x] Create shared `SectionKicker` (pill badge) and `RevealOnScroll` (IntersectionObserver)
- [x] Implement `WaitlistForm` (client component with form state + API integration)
- [x] Mobile-responsive via Tailwind `md:` breakpoint
- [x] Smooth scroll via `scroll-smooth` on `<html>`
- [x] Updated Tailwind config: mockup colors (coral #ff6b4a, ink #1e1206, etc.), neo-brutalist shadows
- [x] Custom CSS: `.eye` gradient text, `.wavy` underline, scroll reveal, keyframe animations
- [x] Fraunces + Nunito loaded via Google Fonts stylesheet link

### Deliverables
- ✅ 15 components in `src/components/landing/` with barrel index
- ✅ Landing page matches HTML mockup design
- ✅ Build, lint, type check all passing

---

## Session 2 — Waitlist Backend + Analytics ✅ (partial)

**Focus:** Wire email capture to Supabase, add PostHog, go live on domain.

### Tasks
- [x] Create API route `POST /api/waitlist` — validates email (Zod v4), inserts to `waitlist` table
- [x] Wire signup form to API route with loading state + success/error feedback
- [x] Duplicate email handling: friendly "You're already on the list!" message (409)
- [ ] Install PostHog (`posthog-js`) and initialize in root layout
- [ ] Track events: `waitlist_signup`, `page_view`, `quiz_preview_started`
- [ ] Set up custom domain in Vercel (manual: neyece.com DNS config)
- [x] Add basic SEO: OG + Twitter card meta tags in root layout
- [ ] Test full flow: visit → signup → confirm → check Supabase row

### Deliverables
- ✅ Waitlist API route with Zod validation and duplicate handling
- ✅ WaitlistForm wired to API with loading/success/error/duplicate states
- ✅ SEO meta tags (OG + Twitter card)
- ⬜ PostHog analytics (deferred — needs package install + project key)
- ⬜ Custom domain (manual Vercel/DNS step)
- ⬜ Full end-to-end test (needs live Supabase connection)

### Component manifest (`src/components/landing/`)

| Component | Type | Description |
|-----------|------|-------------|
| `NeyeceLogo` | Server | Branded "Neyece" with gradient eye |
| `SectionKicker` | Server | Pill badge with color variants |
| `RevealOnScroll` | Client | IntersectionObserver scroll animation wrapper |
| `WaitlistForm` | Client | Email form → POST /api/waitlist |
| `Navbar` | Server | Sticky nav, anchor links, CTA |
| `Hero` | Server | Two-column hero with kicker, headline, form |
| `HeroCard` | Server | Venue preview card (hidden on mobile) |
| `Marquee` | Server | CSS-animated scrolling tagline |
| `HowItWorks` | Server | 3-card feature grid |
| `ProblemSolution` | Server | Old way vs Neyece comparison |
| `QuizPreview` | Client | Interactive pill quiz with vibe results |
| `SocialProof` | Server | 3 testimonial cards |
| `CityShowcase` | Server | Dark bg with city tags |
| `BottomCta` | Server | Bottom CTA with WaitlistForm |
| `Footer` | Server | Dark footer with logo + copyright |

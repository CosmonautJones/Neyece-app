# S1.1 — Auth + Onboarding

**Sprint:** Phase 1 · MVP Core
**Sessions:** 3
**Goal:** Clerk auth integrated, vibe onboarding quiz complete, answers stored to DB.

---

## Session 1 — Clerk Auth Integration ✅

**Focus:** Set up Clerk, protect routes, handle sign-in/sign-up flows.

### Tasks
- [x] Install and configure `@clerk/nextjs`
- [x] Add Clerk middleware for route protection (`src/middleware.ts`)
- [x] Create auth pages: `/sign-in`, `/sign-up` (Clerk's pre-built components styled with Neyece brand)
- [x] Support email + Google sign-in (configured via Clerk dashboard)
- [x] Set up Clerk webhook → create user row in Supabase `users` table on sign-up
- [x] API route: `POST /api/webhooks/clerk` — handles `user.created`, `user.updated`, `user.deleted` events
- [x] VisitorProvider context for accessing current user's DB record (`src/lib/visitor-context.tsx`)
- [ ] Redirect new users to onboarding, returning users to discovery feed *(Session 3)*
- [ ] Test: sign up → user appears in both Clerk dashboard and Supabase *(needs live Clerk + Supabase)*

### Deliverables
- ✅ ClerkProvider in root layout, middleware protecting non-public routes
- ✅ Sign-in/sign-up pages with Neyece brand styling
- ✅ Webhook endpoint syncs Clerk users to Supabase (create/update/delete)
- ⬜ End-to-end test (needs Clerk keys + Supabase connection + webhook tunnel)

### Manual steps remaining
- Add `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY` to `.env.local`
- Add `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in`, `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up` to `.env.local`
- Set up webhook in Clerk dashboard → `CLERK_WEBHOOK_SECRET` in `.env.local`
- Deploy to Vercel or use ngrok for local webhook testing

---

## Session 2 — Vibe Onboarding Quiz (UI) ✅

**Focus:** Build the 4-screen onboarding quiz with state management.

### Tasks
- [x] Create onboarding route group: `src/app/(onboarding)/quiz/page.tsx`
- [x] Build quiz screen components with:
  - ProgressBar (step X of 5, percentage, animated fill)
  - QuizScreen (title, subtitle, multi-select OptionPills)
  - OptionPill (neo-brutalist toggle with shadow/scale animation)
  - Back + Next navigation with disabled state enforcement
  - Slide animations between screens (slideInLeft / slideInRight keyframes)
- [x] Define quiz data structure in `src/lib/quiz-data.ts`:
  - Screen 1: Going-out energy (chill, lively, rowdy, intimate)
  - Screen 2: Ideal setting (rooftop, hidden gem, outdoor, cozy corner)
  - Screen 3: Vibe words (trendy, classic, artsy, local, upscale, casual) — min 2
  - Screen 4: Deal-breakers (loud, no reservations, far transit, expensive) — min 0
- [x] CitySelect screen after quiz (gated to NYC, LA, Chicago, Miami) with card-style grid
- [x] All answers stored in component state (`QuizAnswers` type) during quiz flow
- [x] Submit wired to `POST /api/onboarding/complete` → redirect to `/discover`

### Deliverables
- ✅ 5-step quiz (4 vibe screens + city) navigable with back/next
- ✅ Answers captured in typed state
- ✅ City selection with launch city gating
- ✅ Responsive, branded UI with neo-brutalist style + animations
- ✅ Components: `ProgressBar`, `OptionPill`, `QuizScreen`, `CitySelect` in `src/components/onboarding/`

### Files created
| File | Description |
|------|-------------|
| `src/lib/quiz-data.ts` | Quiz questions, options, city list, answer types |
| `src/components/onboarding/ProgressBar.tsx` | Animated step progress bar |
| `src/components/onboarding/OptionPill.tsx` | Selectable neo-brutalist pill button |
| `src/components/onboarding/QuizScreen.tsx` | Single quiz step with title + pill grid |
| `src/components/onboarding/CitySelect.tsx` | City selection grid (4 launch cities) |
| `src/components/onboarding/index.ts` | Barrel export |
| `src/app/(onboarding)/quiz/page.tsx` | Main quiz page with multi-step state machine |
| `src/app/(onboarding)/layout.tsx` | Onboarding route group layout |

---

## Session 3 — Onboarding Backend + Redirect ✅

**Focus:** Save quiz answers to Supabase, generate initial vibe profile, redirect to feed.

### Tasks
- [x] API route: `POST /api/onboarding/complete` — receives quiz answers + city
- [x] Zod validation of quiz answers (energy, setting, vibes, dealbreakers, city)
- [x] Write answers to `vibe_profiles` table as JSONB
- [x] Generate initial vibe fingerprint vector from answers (18-dim weighted encoding: +1 positive, -1 dealbreakers)
- [x] Update `users` table with selected city
- [x] Mark user as onboarded (flag in user record)
- [x] Redirect to discovery feed on completion
- [x] Handle edge cases: already-onboarded user redirected from quiz to /discover
- [x] Layout-level routing guards: `(onboarding)/layout.tsx` and `(main)/layout.tsx`
- [ ] Test full flow: sign up → quiz → answers in DB → redirected to feed *(needs live Clerk + Supabase)*

### Deliverables
- ✅ `POST /api/onboarding/complete` with Zod validation + Clerk auth
- ✅ Fingerprint vector generator (`src/lib/fingerprint.ts`)
- ✅ Placeholder `/discover` page for onboarded users
- ✅ Layout-level redirect guards (new → quiz, returning → discover)
- ⬜ End-to-end test (needs Clerk + Supabase live)

### Files created
| File | Description |
|------|-------------|
| `src/lib/fingerprint.ts` | Converts quiz answers → 18-dim fingerprint vector |
| `src/app/api/onboarding/complete/route.ts` | POST route: validate, save profile, mark onboarded |
| `src/app/(main)/discover/page.tsx` | Placeholder discover page |
| `src/app/(main)/layout.tsx` | Auth + onboarded guard for main routes |

### Manual steps remaining
- All S1.1 Session 1 manual steps must be complete first

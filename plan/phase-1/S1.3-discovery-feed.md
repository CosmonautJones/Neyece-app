# S1.3 — Discovery Feed

**Sprint:** Phase 1 · MVP Core
**Sessions:** 3
**Goal:** Home screen with mood selector, featured cards, and nearby gems list.

---

## Session 1 — Feed Layout + Venue Cards ✅

**Focus:** Build the main discovery page structure and venue card components.

### Tasks
- [x] Create discovery feed page: `src/app/(main)/discover/page.tsx` (server component with auth guards)
- [x] Build layout: header → mood strip → featured scroll → nearby list → bottom nav
- [x] `VenueCard` component (horizontal scroll variant):
  - Emoji placeholder or hero image with lazy loading
  - Venue name, neighborhood
  - Vibe tag pills (2-3 max)
  - Neyece Score badge (color-coded: coral ≥85, yellow ≥70, white <70)
  - Featured badge variant (larger card, "Featured" pill)
- [x] `VenueListItem` component (vertical list variant):
  - Smaller image thumbnail
  - Name, neighborhood, top vibe tag
  - Score badge
- [x] `BottomNav` component: Home, Search, Saved, Profile (4 tabs, active state)
- [x] `MoodSelector` component: horizontal scrollable pill strip with 7 moods (pulled forward from Session 2)
- [x] `DiscoverFeed` client component: mood filtering, featured/nearby split
- [x] Mobile-first responsive layout
- [x] Mock data: 8 NYC venues with scores + mood-to-tag mapping
- [x] Empty state: "No spots match this mood yet" illustration

### Deliverables
- ✅ `VenueCard` — featured horizontal scroll card with score badge + tag pills
- ✅ `VenueListItem` — compact list item with thumbnail + top tag
- ✅ `BottomNav` — 4-tab fixed bottom navigation with active state
- ✅ `MoodSelector` — scrollable mood pill strip with emoji + coral active state
- ✅ `DiscoverFeed` — client component tying it all together with mood filtering
- ✅ Feed page with header, featured "For You Tonight" section, nearby gems list
- ✅ Empty state for no-match moods

### Files created
| File | Description |
|------|-------------|
| `src/components/discover/VenueCard.tsx` | Scrollable venue card (featured + regular) |
| `src/components/discover/VenueListItem.tsx` | Compact venue list row |
| `src/components/discover/BottomNav.tsx` | Fixed bottom navigation bar |
| `src/components/discover/MoodSelector.tsx` | Horizontal mood filter strip |
| `src/components/discover/DiscoverFeed.tsx` | Main feed layout with filtering logic |
| `src/components/discover/index.ts` | Barrel export |
| `src/lib/mock-data.ts` | 8 mock venues, scores, mood-to-tag mapping |

---

## Session 2 — API + Real Data ✅

**Focus:** Supabase data fetching and replace mock data.

### Tasks
- [x] API route: `GET /api/venues` — Zod-validated query params: `city`, `mood`, `limit`, `offset`
  - Filters by city (required) and mood (optional, maps to vibe tags via `overlaps`)
  - Returns venue data with pagination metadata (`total`, `hasMore`)
  - Proper error responses (400 for validation, 500 for DB errors)
- [x] New `getVenuesFeed` query in queries.ts with `overlaps` filter for vibe tags
- [x] Wire DiscoverFeed to API: tries API on mount, falls back to mock data if unavailable
  - Mood changes trigger new API fetch (or client-side mock filter)
  - "Load more" button for pagination
- [x] Skeleton loading states: `VenueCardSkeleton`, `VenueListItemSkeleton`, `FeedSkeleton`
- [ ] Location-based distance calculation *(deferred to Session 3)*

### Deliverables
- ✅ `GET /api/venues` route with Zod validation + mood→tag mapping
- ✅ `getVenuesFeed` paginated query with `overlaps` vibe tag filter
- ✅ DiscoverFeed wired to API with graceful mock fallback
- ✅ Skeleton loading components matching card/list layouts
- ✅ "Load more" pagination button

### Files created/modified
| File | Description |
|------|-------------|
| `src/app/api/venues/route.ts` | GET route: city + mood + pagination |
| `src/components/discover/Skeleton.tsx` | Card, list item, and full feed skeleton loaders |
| `src/components/discover/DiscoverFeed.tsx` | Updated: API fetch + mock fallback + load more |
| `src/lib/supabase/queries.ts` | Added `getVenuesFeed` with overlaps filter |

---

## Session 3 — Polish + Performance

**Focus:** Final polish and mobile performance.

### Tasks
- [ ] Infinite scroll / "Load more" button for nearby gems
- [ ] Pull-to-refresh on mobile
- [ ] Smooth scroll behavior between sections
- [ ] Performance: image lazy loading, virtualized list for 50+ venues
- [ ] Test on mobile viewport (375px width)
- [ ] Notification bell placeholder in header

### Deliverables
- Full discovery feed working with real data
- Featured + Nearby sections populated
- Smooth mobile experience

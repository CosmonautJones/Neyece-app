# S1.4 — Venue Detail Screen

**Sprint:** Phase 1 · MVP Core
**Sessions:** 2
**Goal:** Full venue detail page with save, share, and placeholder AI insight.

---

## Session 1 — Venue Detail Page

**Focus:** Build the venue detail view with all content sections.

### Tasks
- [ ] Create venue detail page: `/app/venue/[id]/page.tsx`
- [ ] Hero section: full-width image with gradient overlay, venue name, neighborhood
- [ ] Vibe tags displayed as colored pills
- [ ] Info grid: hours, distance, price range, category
- [ ] AI Insight panel (placeholder): "Why this is Neyece for you"
  - Static template for now: "This [category] in [neighborhood] matches your [top vibe] energy."
  - Styled card with subtle gradient border
- [ ] "That's Neyece" primary CTA button (large, coral, prominent)
  - Tracks tap as strong positive signal (writes to future signals table)
- [ ] Back navigation to discovery feed
- [ ] API route: `GET /api/venues/[id]` — returns full venue data
- [ ] Handle 404: venue not found page

### Deliverables
- Venue detail page renders from real Supabase data
- All sections styled to match mockup
- "That's Neyece" button visible and tappable

---

## Session 2 — Save + Share

**Focus:** Save to Saved Spots and share venue as card.

### Tasks
- [ ] Save button (heart icon):
  - `POST /api/venues/[id]/save` — insert to `saved_spots` table
  - Toggle state: saved/unsaved with animation
  - Optimistic UI update
  - Auth required (redirect to sign-in if not logged in)
- [ ] Share button:
  - Native Web Share API (mobile) with fallback to copy-link
  - Share text: "Just found [venue name] on Neyece. That's so Neyece."
  - Share URL: `neyece.com/venue/[id]`
- [ ] OG image for venue share links:
  - Dynamic OG image via Next.js `opengraph-image.tsx`
  - Venue photo + name + Neyece Score badge + brand wordmark
  - Coral/yellow gradient accent
- [ ] API route: `DELETE /api/venues/[id]/save` — remove from saved spots
- [ ] Test: save → see in Supabase → unsave → removed from Supabase

### Deliverables
- Save/unsave working with optimistic UI
- Share generates link with branded OG image
- Full venue detail page complete and polished

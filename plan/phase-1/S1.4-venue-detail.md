# S1.4 — Venue Detail Screen

**Sprint:** Phase 1 · MVP Core
**Sessions:** 2
**Goal:** Full venue detail page with save, share, and placeholder AI insight.

---

## Session 1 — Venue Detail Page ✅

**Focus:** Build the venue detail view with all content sections.

### Tasks
- [x] Create venue detail page: `src/app/(main)/venue/[id]/page.tsx`
- [x] Hero section: gradient background with emoji/image, venue name, neighborhood overlay
- [x] Score badge (color-coded: coral ≥85, yellow ≥70, white <70)
- [x] Vibe tags displayed as colored pills
- [x] Info grid: category, price, neighborhood, city (2-col grid with emoji)
- [x] Description card
- [x] AI Insight panel (placeholder): "Why this is Neyece for you"
  - Dynamic template using category, neighborhood, top vibe tags
  - Styled card with coral-to-yellow gradient border
- [x] Hours section (from Google Places data)
- [x] "That's Neyece" primary CTA button (coral, toggles to yellow "That's So Neyece")
- [x] Back navigation to discovery feed
- [x] API route: `GET /api/venues/[id]` — returns full venue data
- [x] 404 page: "Spot not found" with back-to-discover link
- [x] VenueCard + VenueListItem now link to `/venue/[id]`

### Deliverables
- ✅ `VenueDetail` client component with all sections
- ✅ `GET /api/venues/[id]` route
- ✅ 404 not-found page for missing venues
- ✅ Cards in feed link to venue detail

### Files created
| File | Description |
|------|-------------|
| `src/components/discover/VenueDetail.tsx` | Full venue detail client component |
| `src/app/(main)/venue/[id]/page.tsx` | Venue detail page (server component) |
| `src/app/(main)/venue/[id]/not-found.tsx` | 404 page for missing venues |
| `src/app/api/venues/[id]/route.ts` | GET route: single venue by ID |

---

## Session 2 — Save + Share ✅

**Focus:** Save to Saved Spots and share venue as card.

### Tasks
- [x] Save button (heart icon):
  - `POST /api/venues/[id]/save` — insert to `saved_spots` table
  - `DELETE /api/venues/[id]/save` — remove from saved spots
  - Toggle state with optimistic UI (instant visual flip, revert on error)
  - Handles duplicate saves (23505 error → treat as success)
- [x] Share button:
  - Native Web Share API (mobile) with clipboard fallback
  - Share text: "Just found [venue name] on Neyece. That's so Neyece."
- [x] OG image for venue share links:
  - Dynamic OG image via Next.js `opengraph-image.tsx` (edge runtime)
  - Neyece wordmark, venue name, neighborhood, score badge, tag pills
  - Coral/yellow gradient background with neo-brutalist styling
- [ ] Test: save → see in Supabase → unsave → removed *(needs live Supabase)*

### Deliverables
- ✅ `POST/DELETE /api/venues/[id]/save` with auth + optimistic UI
- ✅ Share via Web Share API with clipboard fallback
- ✅ Dynamic OG image (1200x630) with score badge + tags
- ⬜ End-to-end save/unsave test (needs Supabase)

### Files created
| File | Description |
|------|-------------|
| `src/app/api/venues/[id]/save/route.ts` | POST + DELETE routes for save/unsave |
| `src/app/(main)/venue/[id]/opengraph-image.tsx` | Dynamic OG image for shares |
